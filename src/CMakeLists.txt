#
#

#set(SRCS main.cpp part.h material.h unit.h player.h instruction.h)

#aux_source_directory(parts parts_SRCS)

#message(STATUS "project: ${PROJECT_NAME} srcs: ${SRCS}")
#add_executable(${PROJECT_NAME} ${SRCS} ${parts_SRCS})


